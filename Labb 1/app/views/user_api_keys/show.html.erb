<div class="col-md-6 col-md-offset-3 text-center">
  <h2>Your Key</h2>

  
  <% @apikeys.each do |p| %>
 <p><%= p.value %> <%= p.id %> <%= p.api_user_id%><br> <%= link_to 'Delete User account', apikeyD_path(p.id,p.api_user_id)%></p>
<% end %>

<%= link_to 'ny ApiKey', new_apikey_path(@current_user.id), class: "btn btn-danger btn-lg center" %>
  <%= link_to 'Delete User account', api_user_path(@current_user.id), method: :delete, class: "btn btn-danger btn-lg center" %>
</div>

